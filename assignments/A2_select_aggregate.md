# Assignment A2: SELECT & Aggregation (หลังบทที่ 4)

รัน `A2_seed.sql` ก่อน (สร้างตาราง `tea_orders` 30 แถว: ร้านชานม 2 สาขา 1 สัปดาห์)
ยอดขายของแต่ละออเดอร์ = `qty * unit_price - discount`
ส่งเป็นไฟล์ `A2_<ชื่อ>.sql`: 1 ข้อ = 1 query พร้อม comment เลขข้อ

1. จำนวนออเดอร์ทั้งหมด, จำนวนแก้วทั้งหมด และยอดขายสุทธิรวม (ผลลัพธ์ 1 แถว)
2. 5 ออเดอร์ที่ยอดขายสุทธิสูงสุด แสดง `order_id`, `menu`, `qty`, ยอดสุทธิ
3. ยอดขายสุทธิแยกตาม `channel` เรียงจากมากไปน้อย
4. เมนูที่ขายได้รวม **ตั้งแต่ 8 แก้วขึ้นไป** (ใช้ HAVING)
5. สัดส่วน (%) ของออเดอร์ที่มาจากสมาชิก ทศนิยม 1 ตำแหน่ง
6. ยอดขายสุทธิและจำนวนออเดอร์ **รายวัน** เรียงตามวันที่
7. ในแต่ละ `size` ระดับความหวานไหนถูกสั่ง (จำนวนแก้ว) มากที่สุด แสดงตารางสรุป size × sweetness ทั้งหมด เรียงให้อ่านง่าย
8. คะแนนรีวิวเฉลี่ยและจำนวนออเดอร์ที่ให้คะแนนแยกตามสาขา (ระวัง NULL: อธิบายใน comment ว่า AVG จัดการ NULL อย่างไร)
9. ตาราง pivot: แถว = สาขา, คอลัมน์ = `walk_in`, `grab`, `lineman` (ยอดขายสุทธิ)
10. ออเดอร์ที่ได้ส่วนลดแต่ **ไม่ใช่สมาชิก**
11. แบ่งช่วงเวลา: เช้า (< 11:00), กลางวัน (11:00–13:59), บ่าย (14:00–17:59), เย็น (≥ 18:00) แล้วนับออเดอร์และยอดขายในแต่ละช่วง
12. **(DE)** สร้างตาราง `daily_channel_sales` (sale_date, channel, orders, cups, net_sales) ที่มี primary key เหมาะสม
    แล้วเติมข้อมูลด้วย `INSERT ... SELECT` จาก `tea_orders`
    โบนัส: ทำให้รันซ้ำได้โดยไม่ error และไม่เกิดข้อมูลซ้ำ
